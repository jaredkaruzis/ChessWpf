<UserControl x:Class="ChessWpf.MoveListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d"
             MaxHeight="525">

    <StackPanel Margin="5 -10 7 0" Width="175" MaxHeight="540">

        <Label HorizontalAlignment="Center">Move List</Label>

        <DataGrid ItemsSource="{Binding MoveList}" BorderThickness="1" HeadersVisibility="None" CanUserResizeColumns="False" AutoGenerateColumns="False" IsReadOnly="True" IsHitTestVisible="False">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Index}" Width="25" />
                <DataGridTextColumn Binding="{Binding WhiteMove}" Width="74"/>
                <DataGridTextColumn Binding="{Binding BlackMove}" Width="74"/>
            </DataGrid.Columns>

        </DataGrid>
    </StackPanel>
</UserControl>